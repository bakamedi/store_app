targets:
  $default:
    builders:
      freezed:
        generate_for:
          include:
            - lib/**_model.dart
            - lib/**_response.dart
      json_serializable:
        generate_for:
          include:
            - lib/**_model.dart
            - lib/**_response.dart
